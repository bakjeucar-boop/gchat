# 기술서 7장 자료 — 실측값 목록 초안

> **작업용 임시 파일이다. 기술서 7장 작성이 끝나면 삭제한다.**
> 최종본은 `gchat_기술서.md` 7장이며, 이 파일은 그 재료다.

수집: 2026-08-17 (세션 10)
출처: `api_findings.md` · 계획서 1.2·1.4·1.5절 · git log 커밋 본문 ·
`CLAUDE.md` 진행 상황

**방법 미기록** 표시는 값은 남아 있으나 재현 절차가 저장소에 없는 것이다.
`scripts/probe_models.py`로 잰 값만 재현 가능하고, 세션 7·8의 측정
스크립트는 임시 폴더에서 실행돼 저장소에 없다.

---

## 7.0 측정 도구

| 도구 | 위치 | 무엇을 재는가 |
|---|---|---|
| `scripts/probe_models.py` | 저장소 (567줄) | 모델 스펙·한도·429 구조·토크나이저·그라운딩. `python scripts/probe_models.py sdk` |
| 세션 4~6 측정 | 임시 스크립트 | 실사용 경로를 그대로 태운 턴 수·출력 길이·생성 속도. **저장소에 없음** |
| 세션 7 응답 모드 측정 | 임시 스크립트 (`measure_thinking.py`) | 모드별 입력·사고·출력 토큰과 시간. **저장소에 없음 → `scripts/probe_models.py` 서브커맨드로 복원한다** (세션 10 결정, 7장 작성 때 수행) |
| 세션 7·8 브라우저 실측 | 임시 Streamlit 페이지 + `getBoundingClientRect` | 요소의 화면 좌표·글자 크기. **스크립트를 만들지 않는다** (세션 10 결정) — 12장에 수동 확인 절차로 적는다. 판단 기준은 "모델이 바뀌면 다시 재야 하는가"이고 브라우저 좌표는 아니다 |
| 세션 8·9 화면 판독 | 앱 화면의 "사용량" 표시 | 실제 대화의 입출력 토큰과 시간 |

---

## 7.1 한도

| 값 | 측정일 | 방법 | 결과 | 다시 재야 할 때 |
|---|---|---|---|---|
| **TPM은 입력 토큰만 센다** | 2026-08-13 (세션 2) | 3갈래 실험 — ① 입력 9,012 × 2회 → 429 ② 작은 입력·큰 출력 12개 병렬(출력 19,082) 직후 요청 → 통과 ③ 사고 켬 8개 병렬(사고 5,729, 전체 16,486) 직후 → 통과 | 결정적 증거는 429 메트릭 이름 `generate_content_free_tier_input_token_count` | 등급이 바뀌거나 API가 개편될 때 |
| 사고 토큰은 TPM에 산입되지 않는다 | 2026-08-13 | 위 ③ | 산입 안 됨. 단 `max_output_tokens`에서는 차감 | 〃 |
| Gemma TPM 한도 | 2026-08-13 | 429 본문 `limit: 16000` | 16,000 | 〃 |
| Gemini RPM 한도 | 2026-08-13 | 작은 요청 18개(성공 14 / 429 4) → `quotaValue: "15"` | 15 | 〃 |
| TPM은 60초 슬라이딩 창이며 복구된다 | 2026-08-13 | 429 후 70초 대기 → 11건 성공, 다시 70초 → 8건 성공 | 한도 초과가 모델 차단으로 이어지지 않는다 | 〃 |
| 429 응답 구조 | 2026-08-13 | Gemini RPM 초과 유발 | `RetryInfo.retryDelay`("31s")와 message의 소수("31.691162057s") 양쪽 존재. `quotaId`로 PerMinute/PerDay 구분 | 〃 |
| **retryDelay가 없는 429가 실재한다** | 2026-08-13 | 그라운딩 요청 | `details`에 `Help` 링크뿐 | 〃 |
| RPD 리셋 시각 | — | **미측정** — 하루 이상 관찰 필요 | 문서 근거는 태평양 자정, 세션 내 확인 불가 | 실사용 중 RPD 429를 만났을 때 |

## 7.2 모델 스펙

| 값 | 측정일 | 방법 | 결과 |
|---|---|---|---|
| 컨텍스트 / 출력 상한 | 2026-08-13 | `client.models.list()` (52개 모델 반환) | Gemini 3.5 Flash-Lite 1,048,576 / 65,536 · Gemma 4 (양쪽) 262,144 / 32,768 |
| `thinking_level` 수용값 | 2026-08-13 | 3모델 × 3수준 실호출 | Gemini minimal·medium·high 모두 ✅ / Gemma medium은 **400**, minimal·high는 ✅ |
| **Gemma는 `thinking_config` 생략 시 사고 켜짐** | 2026-08-13 | 생략한 스트리밍 요청 | thoughts 47, 본문 빈 문자열, MAX_TOKENS |
| `usage_metadata` 필드 | 2026-08-13 | 응답 관측 | `prompt`는 항상 / `candidates`·`thoughts`는 **None 가능** / `total`은 셋의 합 |
| 그라운딩 가용성 | 2026-08-13 | `google_search` 도구 부착 | 모델 무관 즉시 429. Gemma는 200이지만 `grounding_metadata`가 None |
| 가격 | — | **조회 불가** — `models.get()`/`list()` 모두 단가를 돌려주지 않는다 | 공개 가격표에서 수동 입력해야 함 |

## 7.3 성능

| 값 | 측정일 | 방법 | 결과 |
|---|---|---|---|
| Gemma 생성 속도 | 2026-08-13 (세션 5) | 6회 평균 | **초당 33.1토큰** (29.8~34.9) |
| Gemma 생성 속도 (재측정) | 2026-08-14 (세션 6) | 3회 | **초당 34.2토큰** — 세션 5와 일치. 코드의 `GENERATION_TOKENS_PER_SECOND`가 이 값 |
| Gemma 생성 속도 (세 번째 확인) | 2026-08-16 (세션 8) | 실사용 1회 (1,063토큰 / 30.6초) | 초당 34.7 — 위 두 값과 일치 |
| Gemma 턴당 소요 시간 | 2026-08-13 (세션 4) | 6턴 연속 대화 | 출력 768 기준 22~25초 |
| **응답 모드별 지연** | 2026-08-15 (세션 7) | 같은 질문을 모드별로 전송. **방법 미기록** (스크립트가 저장소에 없음) | Gemini minimal 4.4초 / medium 6.6초 / high 8.6초 · Gemma minimal 45.5초 / high 57.5초 |
| Gemini 생성 속도 | 2026-08-16 (세션 8) | 실사용 1회 (1,346토큰 / 6.9초) | 초당 약 195 — Gemma의 약 5.6배 |
| 콜드 부팅 시간 | 2026-08-17 (세션 9) | 12시간 슬립 후 첫 접속 | **미측정 (초 단위 기록 없음).** 7장에는 값 대신 이렇게 쓴다 — "콜드 부팅에 상당한 시간이 걸린다. 로그가 멈춘 것처럼 보이는 구간이 길고, 재부팅·새로고침을 반복하면 프로비저닝이 매번 처음부터 다시 시작되어 오히려 더 안 뜬다." 다음 12시간 슬립이 자연히 생기면 그때 재서 채운다 |

## 7.4 토큰

| 값 | 측정일 | 방법 | 결과 |
|---|---|---|---|
| 한국어 자/토큰 | 2026-08-13 | `count_tokens` — 짧은 문장 39자/23토큰, 긴 문단 14,700자/9,002토큰 | **1.63~1.70** → 코드 상수 1.6 (과대 추정 = 안전) |
| 영어 자/토큰 | 2026-08-13 | 69자/19토큰 | **3.63** → 코드 상수 3.5 |
| 세 모델의 토크나이저 동일성 | 2026-08-13 | 같은 입력을 3모델에 `count_tokens` | 오차 1토큰 — **Gemini로 센 값을 Gemma 판정에 그대로 써도 된다** |
| 턴당 컨텍스트 증가 (Gemini, 출력 512 상한) | 2026-08-13 | 5턴 대화 | 약 525~530토큰 |
| 턴당 증가 (Gemma, 출력 768 상한) | 2026-08-13 (세션 4) | 6턴 대화, 앱과 같은 경로 | **782~786토큰**. 1~4턴 모두 768에서 잘림 |
| 턴당 증가 (인스트럭션 유무) | 2026-08-13 (세션 5) | 3턴씩 | 없음 약 1,080 / 있음 약 445 |
| 응답 모드는 입력 토큰에 영향 없음 | 2026-08-15 (세션 7) | 세 모드 동일 질문. **방법 미기록** | 세 모드 모두 입력 **54토큰** — 한도에 전혀 영향 없음 |

## 7.5 답변 길이

| 조건 | 측정일 | 방법 | 평균 출력 | 잘림 |
|---|---|---|---|---|
| 인스트럭션 없음 | 2026-08-13 (세션 5) | 같은 질문 3개, 상한 1,536 | 1,221토큰 (968~1,542) | 1/3 |
| 1차 "2,000자 이내" | 2026-08-13 (세션 5) | 〃 | **435토큰** (지시의 38%) | 0/3 |
| 2차 "1,600~2,400자 + 하한 지시" | 2026-08-14 (세션 6) | 같은 질문 3개, 상한 2,048 | **1,301토큰** (1,144~1,447) | 0/3 |
| 코딩 프리셋 긴 코드 | 2026-08-16 (세션 8) | FastAPI CRUD 요청, 상한 4,096 | Gemini 1,346 / Gemma 1,063 | 0/2 — 상한 근처에도 못 감 |

## 7.6 TPM 병목 경계 (유도값)

| 값 | 산출일 | 방법 | 결과 |
|---|---|---|---|
| 경계 = 평균 답변 ÷ 생성 속도 × 분당 한도 ÷ 60 | 2026-08-15 (세션 7) | `models.tpm_boundary_budget()` | 기본값(1,301토큰 ÷ 34.2)에서 Gemma **9,129** / Gemini **142,653** |
| 답변 길이에 따른 이동 | 2026-08-15 | 같은 식 | 평균 2,000토큰 → 약 14,000 / 510토큰 → 2,877. **상수로는 표현할 수 없는 관계** |
| 실제 병목은 TPM이 아니라 생성 시간 | 2026-08-13 (세션 4) | 6턴 대화 중 `precheck` WAIT 횟수 | **대기 0회**, 창 최대 사용률 29% (4,681/16,000) |

## 7.7 브라우저 실측 (배치 검증)

| 값 | 측정일 | 방법 | 결과 |
|---|---|---|---|
| 멈춤 버튼 위치 | 2026-08-15 (세션 7) | 임시 페이지 + `getBoundingClientRect`. **스크립트 미보존** | 긴 대화에서 `top 2005px` / 화면 720px |
| 확인 창 위치 | 2026-08-16 (세션 8) | 〃 | 7,052px 문서를 끝까지 내린 상태에서 **화면 6,220px 위** |
| 수정 후 확인 창 위치 | 2026-08-16 (세션 8) | 〃 | 드롭다운 148 · 문구 248 · 저장 528 · 전환 584 · 취소 640 (720px 안) |
| Streamlit 기본 글자 크기 | 2026-08-16 (세션 8) | `getComputedStyle` | 버튼 16px(상자 40px) · 설정 라벨 14px · 캡션 14px |
| 목록 제목 상자 높이 | 2026-08-16 (세션 8) | 〃 | 10px일 때 20px(한 줄)/34px(두 줄), 11px 확정 |
| 클립보드 권한 | 2026-08-14 (세션 6) | iframe `allow` 속성과 `isSecureContext` 조사 | `clipboard-write` 있음, secure context true, **`writeText()` 성공은 확인 불가** |
