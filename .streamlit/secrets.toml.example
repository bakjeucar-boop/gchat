# 이 파일을 `secrets.toml` 로 복사한 뒤 값을 채우세요.
# secrets.toml 은 .gitignore 에 있으며 절대 커밋하지 않습니다.
# Streamlit Community Cloud 에 배포할 때는 앱 설정 화면에서 따로 등록합니다.

# Google AI Studio 에서 발급한 API 키
GEMINI_API_KEY = "여기에-API-키"

# 앱 접근용 비밀번호. 20자 이상의 무작위 문자열을 쓰세요 (계획서 2.7절)
APP_PASSWORD = "여기에-20자-이상-무작위-비밀번호"
