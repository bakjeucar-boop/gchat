# 기술서 8장 자료 — 결함 목록 초안

> **작업용 임시 파일이다. 기술서 8장 작성이 끝나면 삭제한다.**
> 최종본은 `gchat_기술서.md` 8장이며, 이 파일은 그 재료다.

수집: 2026-08-17 (세션 10)
출처: git log 39개 커밋 본문 · 결함 수정 커밋 diff · `api_findings.md` ·
계획서 · `CLAUDE.md` 진행 상황 · 회귀 테스트 이름

**근거 미확보** 표시가 붙은 항목은 저장소 안에서 증거를 찾지 못한 것이다.
지어내지 않고 그대로 남겼다 — 세션 기록을 가진 사용자가 채워야 한다.

---

## 8.1 조용히 잘못 도는 부류 (에러가 안 나서 찾기 어려운 것)

| # | 증상 | 원인 | 해법 | 근거 |
|---|---|---|---|---|
| 1 | Gemma 응답 본문이 빈 문자열, `finish_reason=MAX_TOKENS`, `thoughts 47토큰` | `thinking_config`를 생략하면 **Gemma는 사고가 켜진 채로 동작**한다 (Gemini는 반대로 minimal). 사고 토큰이 `max_output` 50을 전부 삼켰다 | 모든 요청에 `thinking_config`를 명시적으로 전송 | `api_findings.md` A-3·A-4 / 커밋 `52b65ac` / `tests/test_client.py:88` `thinking_config를_항상_보낸다` |
| 2 | TPM 추적기가 실제보다 빨리 차서 불필요한 대기를 만든다 | 창 기록에 `total_token_count`(입력+출력+사고)를 쓰려 했다. 서버는 **입력만** 센다 | `record_usage_from()`이 usage 객체에서 `input_tokens`만 뽑는다 — 호출자가 total을 넘기는 실수 자체가 불가능하다 (구조로 차단) | `api_findings.md` A-6 정정 / 커밋 `ab3580d`·`31a7ca7` / `tests/test_quota.py:263` `출력_크기는_TPM_판정에_영향을_주지_않는다`, `:287` `usage_객체에서_입력_토큰만_뽑는다` |
| 3 | 이미 한도가 풀린 요청을 최대 45초 더 막는다 | 창의 기준 시각이 **응답 시각**으로 밀렸다. "마지막 기록을 덮어쓴다"는 구현이 호출 순서에 의존했고, Gemma는 긴 응답 하나에 45초가 걸린다 | `record_sent()`가 기록 핸들(`_Entry`)을 돌려주고 보정은 그 핸들의 값만 갱신한다. 시각은 전송 시각 그대로 남는다 | 커밋 `c9af8f2` / `tests/test_quota.py:313` `창의_기준_시각은_전송_시각이지_응답_시각이_아니다`, `:331`, `:343` |
| 4 | 한국어 문장의 토큰을 실측(23)보다 낮은 21로 추정 — 한도를 넘겨 429를 부르는 **위험한 방향** | 공백을 일괄로 영어 상수(3.5자/토큰)에 할인해 넣었다 | 공백은 **직전 비공백 문자의 분류**를 따른다 | 커밋 `31a7ca7` / `tests/test_context.py:100` `공백은_직전_문자의_분류를_따른다`, `:120` `실측_표본을_과소_추정하지_않는다` |
| 5 | Gemma에 `medium`을 보내면 400 (`Thinking level is not supported`) | Gemma는 켜기/끄기 2단계뿐인데 UI 값이 그대로 전달됐다 | `resolve_thinking_level()`이 전송 **전에** minimal로 되돌린다 | `api_findings.md` A-5 / 커밋 `52b65ac` / `tests/test_client.py:96` `지원하지_않는_사고_수준은_보내기_전에_걸러진다` |
| 6 | 모델 테이블의 Gemma `max_output_tokens`가 8,192 (실제 32,768) | 세션 1의 잠정값을 문서만 보고 채웠다 | 실호출(`models.list()`)로 확인해 정정 | `api_findings.md` A-1·D-1 / 커밋 `52b65ac` (`-8_192 / +32_768`) / `tests/test_models.py:117` `컨텍스트_윈도우와_최대출력이_실측값과_일치한다` |
| 7 | 코딩 답변의 코드가 중간에서 끊긴다 | 길이 목표 인스트럭션(1,600~2,400자)이 코드에도 적용됐고 출력 상한이 2,048이었다 | 코딩 용도는 글자 수 목표를 아예 주지 않고(`GEMMA_CODING_INSTRUCTION`) 상한을 4,096으로 올린다(`max_output_for`) | 커밋 `34eb0a7` / `tests/test_models.py:238` `출력_상한은_용도별이다`, `:266` `코딩_용도에서는_글자수_목표를_주지_않는다` |
| 8 | 답변이 지시의 38% 길이(435토큰)로 과하게 짧아진다 | 인스트럭션이 **상한만** 줬다. 모델은 상한만 주면 계속 아래로 내려간다 | 하한 지시("너무 짧게 줄이지 말고")를 넣어 목표 대역에 묶는다 → 평균 1,301토큰 | `api_findings.md` B-8·B-9 / 커밋 `bd9d160` |

## 8.2 화면에 안 보이는 부류

| # | 증상 | 원인 | 해법 | 근거 |
|---|---|---|---|---|
| 9 | "⏹ 멈춤 버튼이 안 나온다" | 버튼은 스트리밍 내내 렌더링되고 있었다(1.5초 간격 5회 모두 검출). 대화가 길면 **뷰포트 밖**이었다 — 실측 `top 2005px` / 화면 높이 720px. 답변이 자랄수록 더 밀려난다 | `st.container(key="gchat_stop")` + `position: fixed`로 입력창 위에 고정. sticky는 스크롤이 그 지점까지 가야 붙어 부족했다 | 커밋 `6789984` / 계획서 2.7.0절 |
| 10 | "계열 전환 확인 창이 안 뜬다" (새 대화를 누르면 그제야 나타남) | 같은 원인. 확인 창을 본문 맨 위에 그리는데 사용자는 방금 읽은 긴 답변 끝에 있다 — 7,052px 문서를 끝까지 내린 상태에서 확인 창은 **화면 6,220px 위** | 사이드바 **모델 드롭다운 바로 아래**에 그린다(조작한 위젯 옆). 좁은 폭에 맞춰 3버튼을 세로로 쌓았다 | 커밋 `bd93978` / 계획서 2.7.0절 / 5절 4·5·6번 통과 |
| 11 | 멈춤을 누르면 받은 답이 통째로 사라진다 | 멈춤이 리런을 일으켜 성공 경로의 `append_message`가 실행되지 않는다 | `_capture()`가 조각을 `session_state`에 쌓고 다음 실행에서 `stopped_by_user` 메시지로 붙인다 | 커밋 `f1fcf77` / `Message.stopped_by_user` |
| 12 | 멈춘 뒤에도 답이 안 보이고, "계속"을 입력해야 나타난다 | 중단분 병합을 **입력창 단계**에서 했다. 그 시점엔 이력이 이미 그려진 뒤다 | `_commit_interrupted()`를 `render_history` 맨 앞으로 옮겼다 | 커밋 `a18a007` / `gchat/ui/chat.py:48` |

## 8.3 Streamlit 특유의 것

| # | 증상 | 원인 | 해법 | 근거 |
|---|---|---|---|---|
| 13 | 일괄 삭제 후 `StreamlitAPIException` | 체크박스 위젯이 그려진 뒤 그 키에 대입했다 | 대입을 없앴다. 대화가 사라지면 expander 자체가 그려지지 않아 상태도 함께 버려진다 | 커밋 `5f83d98` / `gchat/ui/sidebar.py` `_render_bulk_delete` 주석 |
| 14 | 계열 전환 확인의 확정·취소 버튼에서 `StreamlitAPIException` | 두 버튼이 `ui_model_id`(selectbox 키)를 **사후 대입**했다 | `on_click` 콜백으로 옮겨 다음 실행의 위젯 생성 **전에** 처리한다. "모델 기본값으로 되돌리기"도 같은 이유로 콜백 | 커밋 `a895518` / `gchat/ui/controls.py` `render_family_confirmation` docstring |
| 15 | 인증에 성공해도 비밀번호 입력칸이 화면에 남는다 | 폼이 이미 그려진 뒤 채팅이 그 아래에 붙었다 | 검증 성공 시 `st.rerun()`으로 다시 그려 폼을 지운다 | 커밋 `09d01fb` |
| 16 | 대화 목록 제목 글자 크기가 세 번 고쳐도 그대로 | 라벨은 `button > span > div[data-testid=stMarkdownContainer] > p` 안에 있고 **그 `p`가 font-size를 16px로 재지정**한다. `button`에만 걸면 상자만 줄고 글자는 그대로다 | `button p`에 직접 지정 | 커밋 `5b6c9c2` (앞선 시도 `835d91f`·`f1ef715`) / `CLAUDE.md` 실측 메모 |
| 17 | 대화 목록의 닷(●) 제거가 적용되지 않음 | 포매터가 줄을 다시 감싸 문자열 치환 대상이 어긋났는데 **파일을 다시 확인하지 않고** 커밋했다 | Edit로 정확히 치환하고 파일을 확인. 활성 대화는 key 접두사(`open_active_`)로 구분해 CSS가 굵게 처리 | 커밋 `eee0047` |
| 18 | 질문이 화면에 두 번 보인다 | 본문 상단 캡션이 대화 제목을 찍는데, 제목은 **첫 질문에서 자동 생성**되므로 같은 문장이 겹쳤다 | 캡션 제거. 커스텀 인스트럭션일 때만 한 줄 요약 | 커밋 `f1fcf77` / `gchat/ui/chat.py` `_render_header` |
| 19 | 대화 목록의 ✏️ 🗑 이모지가 버튼 상자 밖으로 삐져나온다 | 칸 비율 `[6,1,1]`이 좁았다 | `[5,1.4,1.4]`로 넓히고 두 아이콘 버튼이 칸을 채우게 했다 | 커밋 `f1fcf77` |
| 20 | 비밀번호 5회 잠금이 오작동한다 — 제출하지 않은 리런에서도 실패 횟수가 늘어난다 | key 없는 `text_input`은 **리런마다 같은 값을 되돌려준다.** 계획서 2.7절 예시 코드처럼 반환값을 그 자리에서 판정하면, 제출과 무관한 리런이 매번 "틀린 비밀번호 입력"으로 집계된다 | `st.form`으로 감싸 **제출 시점에만** 판정한다 (`clear_on_submit=True`) | 커밋 `f28febd` (세션 1) / `gchat/auth.py:82` 주석과 `:85` `st.form` / `tests/test_auth.py:57` `실패_5회에_잠긴다` |

## 8.4 화면 문구 결함

| # | 증상 | 원인 | 해법 | 근거 |
|---|---|---|---|---|
| 21 | 화면에 "(계획서 1.4절)", "서버 429가 최종 진실", 원본 400 JSON이 노출 | 개발 문서 어휘와 사용자 화면 어휘를 분리하지 않았다 | 화면 문구 전면 정리. 근거는 주석·docstring에만. 원문은 "자세한 내용" 접기 안으로 | 커밋 `a18a007` / 계획서 2.7.1절 |
| 22 | 라벨과 값의 단위가 다르다 — "분당 **글자** 5,400"인데 값은 토큰 (한국어 1토큰 ≈ 1.6자라 **약 1.6배 틀린 표시**) | 개발 용어를 걷어내면서 단위를 주장하는 이름으로 바꿨다 | 단위를 주장하지 않는 "분당 사용량"으로. 메시지 메타 "글자분"도 제거 | 커밋 `f2a72fd` / 계획서 2.7.1절 |
| 23 | "앞선 **대화** N개는 참고되지 않습니다" — N은 메시지 수인데 사이드바의 "대화 N개"는 대화 수 | 같은 단어가 서로 다른 것을 세고 있었다 | "앞선 **메시지** N개"로 (3곳) | 커밋 `f2a72fd` |

## 8.5 배포·플랫폼·도구

| # | 증상 | 원인 | 해법 | 근거 |
|---|---|---|---|---|
| 24 | 12시간 슬립 후 첫 접속에서 로딩만 오래 돈다 | 콜드 부팅이 컨테이너 생성부터 의존성 설치까지 처음부터 다시 한다 | **기다린다.** 재부팅·새로고침을 반복하면 프로비저닝이 매번 처음부터 다시 시작돼 오히려 안 끝난다 | 계획서 6절 운영 메모 (세션 9 실측) |
| 25 | 배포 로그에 `More than one requirements file detected` | `requirements.txt`와 `pyproject.toml`이 함께 있다 | 현재는 uv가 `requirements.txt`를 고르고 있어 문제없다. 빌드 도구가 바뀌면 엉뚱한 쪽을 잡을 수 있으니 기억해 둘 것 | 계획서 6절 운영 메모 |
| 26 | `git push`가 에이전트 셸에서 끝나지 않는다 (5분 타임아웃) | Git Credential Manager가 GUI 로그인 창을 띄우는데 비대화형 셸이 응답할 수 없다 | 최초 1회는 사람이 직접 실행. 이후에는 자격 증명이 캐시돼 창 없이 진행된다. 에이전트는 `git ls-remote`로 결과만 검증 | 계획서 6절 · `CLAUDE.md` 배포 정보 (세션 8 실측) |
| 27 | (제외) 검증 스크립트의 종료 코드 오탐 | 양쪽 기록 어디에도 근거가 없어 **사례집에서 뺀다.** 재현·검증되지 않는 사례는 문서 신뢰도만 깎는다 | 남길 것은 한 줄 주의뿐 — 파이프라인의 종료 코드는 **마지막 명령**의 것이므로, 검색 결과 유무를 종료 코드로 판정하지 말고 **개수로 판정할 것** | 근거 미확보 (사례 아님, 주의 사항으로만) |

## 8.6 문서·절차 결함

| # | 증상 | 원인 | 해법 | 근거 |
|---|---|---|---|---|
| 28 | 계획서 2.8절이 "상세 설정에서 늘리세요"라고 안내하는데 그 UI가 없다 | 최대 출력 UI를 v1에서 뺀 뒤 문구가 남았다 | 모순 문구 제거, 503·빈 응답·잘못된 키 항목 복원 | 커밋 `ab3580d` |
| 29 | 계획서 5절 검증 항목 번호 22·23이 누락 | 문서 편집 중 유실 | 번호 복구 | 커밋 `ab3580d` |
| 30 | 계획서를 덮어쓰면서 앞서 동기화한 부분이 원복됐다 (부록 A 환경 2줄, 그라운딩 제외, TPM 원칙 줄) | 전체 덮어쓰기 방식이라 부분 갱신이 유실된다 | 덮어쓰기 후 부록 A ↔ CLAUDE.md를 diff로 재대조하는 절차를 매번 수행 | 커밋 `ab3580d`·`eaae140` |
| 31 | "계획서를 덮어썼다"는 말을 믿고 작업을 시작했는데 파일이 실제로는 이전 판이었다 | 사용자가 덮어썼다고 말한 시점과 실제로 저장한 시점이 어긋났다. 세션 2에서 처음 났고(8/13 23:12판 — 2.4.1절·검증 29~34번·부록 B 7·9번이 모두 없었다), 세션 8·9에서도 재현됐다 | **문서를 받았다고 가정하지 않고 내용으로 확인한다.** 반영 전에 `git status`로 변경 여부를, 새 절의 특징 문자열 `grep`으로 도착 여부를 먼저 본다. 없으면 그 사실을 먼저 알리고 기다린다 | 세션 2 보고 (사용자 기록) / 세션 8·9에서 이 절차로 미도착을 즉시 발견 — 그 뒤로 재발 없음 |

## 8.7 자동화로 판정할 수 없었던 것

| # | 항목 | 왜 자동화가 안 되는가 | 최종 판정 | 근거 |
|---|---|---|---|---|
| 32 | 클립보드 복사 | `navigator.clipboard.writeText()`는 문서 포커스를 요구한다. 자동화 환경에서는 `NotAllowedError: Document is not focused` | 사람이 눌러 **통과** (세션 8 로컬, 세션 9 HTTPS) | `api_findings.md` B-9 항목 9 / 계획서 5절 33번 |
| 33 | 시각적 배치 (버튼이 실제로 보이는가) | DOM에 존재하는 것과 화면에 보이는 것이 다르다. 결함 9·10이 정확히 이 틈에서 났다 | 브라우저에서 좌표를 재는 방식으로 판정 가능해짐 (`getBoundingClientRect`) | 커밋 `6789984`·`bd93978` |
| 34 | 문장 자연스러움 (멈춤 후 "계속"으로 이어받은 글) | 사람이 읽어야 안다 | **미판정** (부록 B 열린 사항 10) | 계획서 부록 B |

---

## 되돌린 결정 (6장 자료로 함께 수집)

| 결정 | 처음 | 되돌린 뒤 | 이유 | 근거 |
|---|---|---|---|---|
| 설정 메뉴 위치 | 사이드바 → 입력창 위 컨트롤 바(popover) | **사이드바 expander로 복귀** | 대화 중 모델을 바꿀 일이 거의 없고, popover는 상호작용마다 리런이 걸려 무겁다 | `32e9fa7`·`bd9d160` / 계획서 2.6절 |
| 웹 검색 그라운딩 | v1 포함 | **v1 제외** | 무료 티어에서 호출 즉시 429. 켤 때마다 실패하는 스위치가 된다 | `52b65ac` / `api_findings.md` C절 |
| 메시지 복사 방식 | `st.code` 2단계 | **`navigator.clipboard` 한 번 클릭** | iframe에 `clipboard-write` 권한이 실제로 있음을 확인 | `09d01fb` / B-9 |
| 휘발성 경고 | 상단 고정 → 내보내기 위 | **화면에서 제거** | 사용자 요청. 문구 상수만 남김 | `09d01fb` / 계획서 2.4절 |
| 계열 전환 | 확인 없이 자동 전환 → 3버튼 확인 | **확인 유지, 위치만 사이드바로** | 확인은 필요하지만 본문 상단은 안 보인다 | `5f83d98`→`475a7b8`→`bd93978` |
| 되돌리기 지속 시간 | "5초" | **다음 조작까지** | Streamlit은 상호작용 없이 화면을 다시 그리지 않는다 | `5f83d98` |

## 여러 번 고친 값의 궤적 (6.3장 자료)

| 값 | 궤적 | 최종 근거 |
|---|---|---|
| Gemma `default_max_output` | 2,048 → 768 → 1,536 → 2,048 → 4,096 → **2,048 + 코딩만 4,096** | `52b65ac`→`9b9ce6b`→`bd9d160`→`34eb0a7`→`8895954` |
| Gemma `default_context_budget` | 3,000 → **9,000** | `9b9ce6b` (세션 4 실사용: TPM 대기 0회) |
| TPM 병목 경계 | 12,000 → 11,000 → 9,000 → **유도식** | `475a7b8`→`bd9d160`→`8895954` |
| 답변 길이 인스트럭션 | 없음 → "2,000자 이내" → **"1,600~2,400자 + 하한 지시"** | B-8 → B-9 |
| 턴당 토큰 추정 | 600 → 1,750 → 용도별 상수 → **대화 실측 평균** | `8895954` |
| 목록 제목 글자 크기 | 16px → 14px → 12px → 10px → **11px** | `835d91f`→`f1ef715`→`5b6c9c2`→`f2a72fd` |
