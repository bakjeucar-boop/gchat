# gchat

Streamlit 기반 개인용 AI 챗봇. Google Gemini API 사용.

## 원칙
- `docs/gchat_계획서.md`가 단일 진실 원천이다. 다르게 구현할 이유가 생기면
  임의로 바꾸지 말고 근거를 보고하고 승인을 받은 뒤 계획서를 갱신한다.
- 모델별 능력·한도는 `gchat/models.py`의 테이블에만 기술한다.
  UI·client·quota는 이 테이블을 참조할 뿐 값을 하드코딩하지 않는다.
- 응답은 항상 스트리밍한다.
- 오류를 조용히 삼키지 않는다. 무엇이 왜 실패했는지 사용자에게 보인다.
- 한도 추적기는 추정치이고 서버 429가 최종 진실이다. 둘이 어긋나는 경우를
  정상 동작으로 간주해 처리한다.
- Gemma에는 모든 요청에 `thinking_config`를 명시적으로 보낸다. 생략하면
  사고 모드로 동작해 TPM은 아니지만 출력 예산을 갉아먹는다 (세션 2 A-4).
- TPM은 입력 토큰만 센다 (세션 2 실측). TPM 추적에는 `prompt_token_count`만 쓰고,
  `total_token_count`는 비용·표시용으로만 쓴다.
- secrets를 로그나 화면에 출력하지 않는다.
- **즉시 반응해야 하는 UI(멈춤 버튼·확인 창·오류 배너)를 본문 흐름에 그대로
  얹지 않는다** (2.7.0절). `position: fixed`로 고정하거나 조작한 위젯 옆에
  그린다. 대화가 길어지면 사용자는 늘 문서 맨 아래에 있어, 본문 상단에 그린
  것은 화면 밖이 된다 — 세션 7·8에서 같은 결함이 두 번 나왔다.
  **긴 대화(메시지 20개 이상) 상태에서 검증할 것.**
- 화면 문구에 계획서 절 번호·내부 변수명·HTTP 상태 코드·원본 오류 JSON을
  쓰지 않는다 (2.7.1절). 근거는 주석에, 화면에는 결과만.
- 라벨을 사용자 언어로 바꿀 때 **표시되는 숫자의 단위가 라벨과 맞는지**
  확인한다. 토큰을 "글자"라고 부르면 약 1.6배 틀린 표시가 된다.

## 환경
- Windows 11, Python 3.12, 전역 pip 설치 (venv·uv 미사용 — 세션 1에서 확정)
- 작업 폴더: C:\Users\user\Documents\claude_code\gchat
- 테스트: pytest / 린트: ruff (`requirements-dev.txt`에는 이 둘뿐 — mypy 없음)

## v1 제외 (구현하지 말 것)
- (개정됨) 설정은 사이드바 "설정" expander에 둔다. 입력창 위 배지는
  표시 전용이며 조작 요소를 넣지 않는다 (2.6절)
- 최대 출력 토큰 UI. 모델별 고정 상수이며 TPM 계산용으로만 쓴다
- 대화 영구 저장 (DB, 파일 저장). 대화는 session_state에만 존재한다
- 파일 첨부 (2.9절 참고만)
- 웹 검색 그라운딩 (2.10절 — 무료 티어 API 자체가 차단, 세션 2 실측 확정).
  `tools` 파라미터를 아예 쓰지 않는다
- Google OIDC 로그인
- 컨텍스트 요약 압축

## 금지
- `.streamlit/secrets.toml` 커밋
- temperature / top_p / top_k / candidate_count 전달 (어느 모델에도, 1.2절 결정)
- 요청 contents의 마지막 턴을 model 역할로 끝내기
- localStorage/sessionStorage 사용
- 서버 파일시스템에 대화 저장 (내보내기는 download_button만 사용)

## 진행 상황
- 세션 1 완료: 골격, `models.py`, `state.py`, `auth.py`, 최소 화면, 테스트
- 세션 2 완료: 실측(`scripts/probe_models.py` → `docs/api_findings.md`),
  `models.py` 실측 반영, 계획서 1.4·1.5·2.10절 갱신, `client.py`
- 세션 3 완료: `quota.py`(QuotaTracker·QuotaBook·429 보정) + `context.py`
  (토큰 추정·경계 실호출·예산 절단). 가짜 시계 테스트 150건
- 세션 4 완료: `ui/chat.py` + `ui/sidebar.py` + `app.py` 배선. 앱이 실제로
  대화한다. 배선 순서는 `fit_to_budget` → `precheck` → `record_sent` →
  `stream` → `record_usage_from(entry)`
- 세션 5 완료: `ui/controls.py` 컨트롤 바(배지 + popover), 한도 게이지 진행바,
  계열 전환 확인 3버튼, 모델별 기본 시스템 인스트럭션 배선
- 세션 6 완료: 세션 5 피드백 8건 반영(사이드바 설정 복귀·빈 대화 방지·제목
  축약·경고 이동·메시지 복사), `export.py` + `ui/export_ui.py`,
  자동 검증 27항목 통과
- 세션 6 실사용 피드백 2차(같은 날): 인증 후 비밀번호 칸 잔존 수정, 사이드바
  순서 재배치, 휘발성 경고 제거, 제목 편집 인라인화, 원클릭 복사,
  **용도 프리셋(범용·코딩·커스텀)**, **멈춤 버튼**, 코딩 답변 잘림 해소
- 세션 7 완료 (1단계): 코드가 계획서를 따라잡는 3건 — 용도별 출력 상한
  (`max_output_for`), 턴 수를 그 대화의 실제 평균으로 계산(`tokens_per_turn`),
  대기 경고 기준선을 상수 대신 **유도**(`tpm_boundary_budget` = 평균 답변 ÷
  생성 속도 × 분당 한도)
- 세션 7 피드백 반영: 멈춤 버튼 `position: fixed` 고정, 중단분 병합을
  `render_history` 앞으로 이동, **화면 문구 전면 정리**(2.7.1절),
  Gemini 기본 응답 모드 `medium`, 대화 목록 모양(10px·왼쪽 정렬·닷 제거·
  ✏️ 가운데)
- 세션 7 배포 사전 점검 통과: `.gitignore` 차단 확인, API 키·비밀번호를
  추적 파일과 **전체 커밋 이력**에서 전수 검색해 0건, `requirements.txt`
  충분, README 절차 일치 → 공개 저장소로 가도 이력 정리 불필요
- 세션 8 완료: **배포까지 끝났다.**
  - 단위를 주장하는 라벨 정리 — "분당 글자" → **"분당 사용량"**, 메시지 메타
    "글자분" 제거, "앞선 대화 N개" → "앞선 **메시지** N개"(N이 메시지 수였다)
  - 육안 확인 2건 해소 — 복사 붙여넣기 통과(부록 B 9), 코딩 긴 코드는
    Gemini 1,346 / Gemma 1,063 토큰으로 **양쪽 다 잘림 없음**(부록 B 11).
    상한 4,096 근처에도 못 가 "코딩은 Gemini 권장" 안내는 넣지 않았다
  - **계열 전환 확인 창이 안 뜨는 결함** — 멈춤 버튼과 같은 원인이었다
    (본문 상단에 그려 화면 6,220px 위). 사이드바 모델 드롭다운 아래로 옮겼다.
    이 두 건이 계획서 2.7.0절 배치 원칙이 됐다
  - 배포 완료 — 앱 https://rvxu3cqzajtbassupttgph.streamlit.app/ ·
    저장소 https://github.com/bakjeucar-boop/gchat (공개)
  - 대화 목록 제목 10px → 11px

- 세션 9 완료: **배포 환경 확인 3건 전부 통과 → 계획서 5절 38항목 소진.**
  - 27번 12시간 슬립 — 잠들었다가 콜드 부팅으로 깨어났고(로그에 컨테이너
    재생성·의존성 재설치가 찍힌다) 대화는 전멸했다. 계획서대로다
  - 33번 복사 — **HTTPS 배포 환경에서 통과.** 로컬 localhost 는 secure
    context 가 자동 통과라 이번이 진짜 판정이었다
  - 4·5·6번 계열 전환 확인 — 메시지 20개 이상 긴 대화에서 확인 창이
    스크롤 위치와 무관하게 보였다. 2.7.0 배치 원칙이 실제로 통했다
  - 코드 변경 없음. 남은 것은 개발 작업이 아니다 (RPD 리셋 시각 관찰,
    가격 필드 수동 입력, 부록 B 8·10)

## 배포 정보
- 앱: https://rvxu3cqzajtbassupttgph.streamlit.app/
- 저장소: https://github.com/bakjeucar-boop/gchat (공개, `origin/main`)
- Cloud 설정: main 파일 `app.py`, Python 3.12, Secrets 에 `GEMINI_API_KEY`·
  `APP_PASSWORD` (TOML 이라 따옴표 필수)
- push 는 GitHub 로그인 창을 띄우므로 **에이전트 셸에서는 끝나지 않는다.**
  사용자가 직접 실행하고, 에이전트는 `git ls-remote` 로 결과만 검증한다

## 남은 것 (개발 작업 아님)
1. **RPD 리셋 시각** — 문서 근거는 태평양 시간 자정. 실사용 중 RPD 429 를
   만나면 `quotaId` 와 `retryDelay` 를 기록해 확정한다 (부록 B 열린 사항 1)
2. **가격 필드** — `price_in/out_per_mtok` 는 API 로 조회되지 않아 공개
   가격표를 보고 수동으로 채워야 한다
3. 부록 B 8(턴 수 표시의 체감 정확도) · 10(멈춤 후 "계속" 이어받기의
   자연스러움) — 둘 다 실사용에서 느낌으로 판정할 항목이다

## 계획서 ↔ 코드 차이 (2026-08-17 갱신본 기준)
**차이 없음.** 세션 8 마지막에 세 건이 모두 닫혔다.

- 2.7.1 라벨 단위 → "분당 사용량"으로 코드 반영
- 1.5 경계값 → 계획서가 "9,100은 상수가 아니라 계산 결과"임을 명시.
  코드는 하드코딩 0건이고 `tpm_boundary_budget`로 매번 유도한다
  (Gemma 9,129 / Gemini 142,653, 평균 답변 길이가 바뀌면 함께 움직인다)
- 2.4 휘발성 경고 → 화면에서 뺀 현행 유지가 사용자 결정으로 확정
- 부록 A 2.7.0 배치 원칙 → 계획서에 반영됐고 CLAUDE.md 와 문구까지 일치

- 세션 4~5 결정: 되돌리기는 "5초" 대신 **다음 조작까지** 유지(Streamlit 은
  상호작용 없이 다시 그리지 않는다). 계열 전환 확인의 Markdown 저장 버튼은
  세션 6 까지 비활성
- 남은 미해결: RPD 리셋 시각(부록 B "열려 있는 사항" 1),
  가격 필드(수동 입력 필요)
- 실측으로 해소된 것 (`docs/api_findings.md`)
  - B-7 (세션 4): Gemma 턴당 증가 782~786 토큰 — 이 관측이 "TPM 은 실제로
    걸리지 않는다"로 이어져 기본값을 9,000·1,536 으로 상향하게 했다
  - B-8 (세션 5): 1차 인스트럭션이 출력을 64% 줄였으나(1,221 → 435 토큰)
    너무 짧았다. 생성 속도는 초당 33.1 토큰
  - B-9 (세션 6): 재개정 문구가 목표 구간에 안착했다 — 평균 1,301 토큰,
    잘림 0/3. 하한 지시("너무 짧게 줄이지 말고")가 결정적이었다.
    클립보드는 iframe 권한은 있으나 자동화로 확인 불가여서 st.code 를 채택
  - 세션 7: 응답 모드는 한도에 **전혀** 영향이 없다 — 세 모드의 입력 토큰이
    54로 동일했다. 차이는 응답 시간뿐(4.4 / 6.6 / 8.6초). Gemma 는 사고를
    켜면 사고 745 + 답변 1,300 = 2,045 로 상한 2,048 에 걸려 답이 잘린다
- Streamlit 버튼 라벨은 `button > span > div[data-testid=stMarkdownContainer]
  > p` 안에 있고 그 `p` 가 font-size 를 16px 로 다시 잡는다. `button` 에만
  크기를 걸면 상자만 줄고 글자는 그대로다 (세션 7 실측, 두 번 헛짚었다)
